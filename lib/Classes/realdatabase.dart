class RealData {
  String title ,docid_cat  , rating, category , info;
  RealData( this.title , this.docid_cat, this.rating, this.category, this.info);
}

class SearchData {
  String title ,docid_cat ,  rating, category, info;
  SearchData( this.title , this.docid_cat, this.rating,  this.category,this.info);
}
class AllData {
  String title ,docid_cat  , rating , category, info;
  AllData( this.title , this.docid_cat, this.rating, this.category, this.info);
}
